#include <stdio.h>

//Compiler version gcc  6.3.0

int main()
{
  int n,rev=0;
  int rem;
  printf("Enter the number:\n");
  scanf("%d",&n);
  while(n!=0) 
  {
    rem=n%10;
    rev=(rev*10)+rem;
    n=n/10;
  }
  printf ("%d",rev);
  
  return 0;
}
